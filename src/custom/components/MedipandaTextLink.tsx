import { Link } from '@mui/material';
import { styled } from '@mui/material/styles';
import { colors } from '../../globalStyles.ts';

export const MedipandaTextLink = styled(Link)({
  color: colors.vividViolet,
}) as typeof Link;
